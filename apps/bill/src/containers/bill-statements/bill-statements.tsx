import React, { useState } from 'react';

// Native Base Components
import {
  useTheme,
  Pressable,
  Box,
  Text,
  Badge,
  HStack,
  ScrollView,
  Spacer,
  Button,
} from 'native-base';

// Icons
import { InfoCircle, ChevronRight } from '@cd-next-gen-app/icons';

const BillStatement = ({ navigation }: { navigation: any }) => {
  return (
    <Box>
      <Text>BILL STATEMENTS</Text>
    </Box>
  );
};
export default BillStatement;
